# Hack Assembler (nand2tetris, Chapter 6)

## Dev setup
```bash
# from repo root
source .venv/bin/activate
pytest
hackasm --help
```

## Usage
```bash
hackasm path/to/Prog.asm -o path/to/Prog.hack
```
